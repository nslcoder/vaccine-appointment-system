<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>VAS - Vaccine Appointment System</title>
    <link rel="stylesheet" href="/styles.css" />
</head>
<body>
    <div class="bg-gray-100 h-screen p-5 flex flex-col items-center">
        <div class="border-2 rounded shadow-lg p-5 bg-white">
            <div class="text-center border-b border-gray-600 pb-6">
                <h1 class="font-bold"><%= name %></h1>
                <h2 class="text-sm"><%= email %></h2>
                <form action="/users/logout" class="mt-5">
                    <button class="bg-gray-300 px-2 py-1 rounded">Logout</button>
                </form>
            </div>

            <div class="mt-5">
                <div class="text-center mb-3">
                    <h1 class="mb-1">Don't have an appointment?</h1>
                    <form action="/users/dashboard/appointments" metho="get">
                        <button class="bg-green-400 p-2 font-semibold text-white rounded w-full">Create</button>
                    </form>
                </div>
                <div class="text-center">
                    <h1 class="mb-1">Already have an appointment?</h1>
                    <form action="/users/dashboard/appointments/<%= username %>/<%= userId %>" method="get">
                        <button class="bg-green-400 p-2 font-semibold text-white rounded w-full mb-2">Edit</button>
                    </form>

                    <form action="/users/appointments/pdf/<%= username %>/<%= userId %>" method="get">
                        <button class="bg-green-400 p-2 font-semibold text-white rounded w-full">View</button>
                    </form>
                </div>
            </div>
        </div>
    </div>      
</body>
</html>